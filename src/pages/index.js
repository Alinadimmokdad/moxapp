import Parent from "@/containers/auth/Parent";
import React from "react";

const index = () => {
  return <Parent />;
};

export default index;
// componnets folder for each component
// component for auth layout
// design grid container + item
// button loading component
// on forget setTimeOut for slicing between sign in and reset
// special character on validation for password
// also mock data for forget password\
